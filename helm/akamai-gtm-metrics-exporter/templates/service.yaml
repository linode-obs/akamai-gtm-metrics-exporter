apiVersion: v1
kind: Service
metadata:
  name: {{ include "akamai-gtm-metrics-exporter.fullname" . }}
  labels:
    {{- include "akamai-gtm-metrics-exporter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "akamai-gtm-metrics-exporter.selectorLabels" . | nindent 4 }}
